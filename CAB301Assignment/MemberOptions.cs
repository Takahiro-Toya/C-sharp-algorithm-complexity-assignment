using System;
namespace CAB301Assignment {
    public static class MemberOptions {


        public static string Member2(Movie[] movies) {
            // display all movie title in the store

            // ask which title they want to borrow (input by number)

            // return title
            return null;
        }

        public static int Member3(Movie[] borrowing) {
            // display all movies that they are borrowing (given by parameter)

            // ask which movie they want to return (input by number)

            return -1;
        }
    }
}
